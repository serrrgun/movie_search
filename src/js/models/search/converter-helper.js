export default class Movie {
  constructor(title, posterUrl, date, rating, id) {
    this.title = title;
    this.posterUrl = posterUrl;
    this.date = date;
    this.rating = rating;
    this.id = id;
  }
}
